# موقع محمد الحسين الشخصي — دليل المشروع (لكلود وللمستخدم)

> هذا الملف يشرح المشروع وكيفية تعديله. اقرأه كامل قبل أي تعديل.

## نبذة
موقع بورتفوليو شخصي لـ **محمد الحسين** (مبرمج ومهندس ذكاء صناعي).
- **الفكرة:** روبوت 3D خلفية على اليمين + كلام على اليسار، وتنقّل بين الصفحات بتمرير اتجاهي (يمين/تحت/يسار/فوق) متل تقليب شاشات الموبايل.
- **المحتوى بعامية شامية**، **عناوين اللغات بالإنجليزي**.
- **13 صفحة:** الرئيسية، HTML، CSS، JavaScript، Dart، Three.js، Python، PHP، C، AI Engineering، AI Training، Projects، Contact.
- تقنية بسيطة: **HTML/CSS/JS خام** (بدون build ولا npm) + Three.js من CDN.

## كيف تشغّل الموقع
FBX والصوت لازم يُقدّموا عبر سيرفر (مش بفتح الملف مباشرة file://). من داخل مجلد المشروع:
```bash
python -m http.server 8080
```
ثم افتح: `http://localhost:8080/index.html`

## بنية الملفات
```
index.html            # الصفحة الرئيسية (كل الأقسام الـ13)
css/style.css         # كل التنسيق + الهوية (ألوان، خطوط، انتقالات)
js/
  robot.js            # الروبوت 3D (Three.js): تحميل، حركات، تتبّع الماوس بالراس، إضاءة جانبية ملوّنة
  nav.js              # التنقّل (تمرير أفقي من اليسار دائماً) + ألوان التمييز لكل صفحة
  background.js        # خلفية الدارات المتحركة (Canvas) — لون فضي ثابت (مفصول عن الأكسنت)
  cursor.js           # مؤشّر الماوس المخصّص (نقطة + حلقة)
  fx.js               # بهرجة: مقدّمة (كتابة الاسم) + عدّادات أرقام + إمالة 3D لبطاقات المشاريع
assets/robot/*.fbx    # 13 حركة Mixamo (Y Bot، With Skin)
design-previews/      # صفحات معاينة استُعملت وقت التصميم (خطوط، لوغو، خلفيات) — مراجع فقط
PLAN.md               # خطة المشروع الأصلية
```

## المكتبات الخارجية (كلها مجانية، عبر الإنترنت)
- **Three.js 0.160** + FBXLoader — من `cdn.jsdelivr.net` (importmap في `index.html`).
- **خطوط Google:** Space Grotesk (عناوين)، JetBrains Mono (تسميات/أرقام)، Cairo (عربي).
- **لوغوهات اللغات:** Simple Icons — `https://cdn.simpleicons.org/{slug}/4d9fff` (ملاحظة: CSS=`css` مش `css3`؛ تجنّب `openai` استعمل `tensorflow`).
- **الأصوات:** نُزّلت من مكتبة Google الصوتية المجانية (actions.google.com/sounds/v1).

## الهوية (مهمة — التزم فيها)
- **غامق هادي** `#0b0f17` + **لون تمييز واحد يتغيّر لكل صفحة** (مش نيون ملوّن ثابت).
- **الخطوط:** عناوين Space Grotesk، تسميات/أرقام JetBrains Mono، نص عربي Cairo.
- **المحتوى بعامية شامية.** العناوين إنجليزي.
- الروبوت لونه الأساسي محفوظ تماماً؛ لون الصفحة بس إضاءة جانبية (من اليسار) عليه.

## وصفات التعديل (Recipes)

### حجم/مكان/تأطير الروبوت
`js/robot.js` → `CFG` بالأعلى:
- `scale` أكبر = روبوت أكبر · `modelY` أعلى = يرفعه · `modelX` أكبر = يزيحه يمين
- `camera.{y,z,lookY}` للتأطير · `look.{yaw,pitch}` قوة تتبّع الراس للماوس
- بعد التعديل من الكونسول للتجربة الحيّة: `applyRobotCFG()`

### حركة الروبوت لكل صفحة
`js/robot.js` → مصفوفة `PAGE_ANIM` (13 عنصر، ترتيبها = ترتيب الصفحات). القيم أسماء مفاتيح من `CFG.clips` (أو `wave`).
- كل حركة تنعمل **مرة وحدة وتتجمّد** بوضعية وقوف طبيعية، والراس يتبّع الماوس.
- الضغط على الروبوت = يرقص (`hiphop`)، وضغطة تانية يوقف. (raycasting في `robot.js`).

### لون التمييز لكل صفحة
`js/nav.js` → مصفوفة `ACCENT` (13 لون hex). `applyAccent(i)` يبدّل متغيّرات CSS (`--accent`, `--accent-2`, `--accent-soft`) + لون الإضاءة الجانبية للروبوت + الخلفية.
- الروبوت **يحتفظ بلونه الأصلي**؛ لون الصفحة بيجي فقط من الضوء الجانبي (يسار) `rim1` في `robot.js` عبر `window.robotAccent(hex)` — ما في تلوين لكامل الجسم (emissive).

### التنقّل
`js/nav.js`: تمرير أفقي **دائماً من اليمين** — الصفحة الجديدة تدخل من اليمين للأمام (`fwd='translateX(100%)'`)، وبالرجوع تنعكس. (لا خريطة تنقّل ولا اتجاهات متعدّدة.)

### الهيدر + توسيط المحتوى
- **هيدر علوي زجاجي ثابت** (`#topnav`، يُبنى بـ `nav.js`): كل الصفحات الـ13 كأزرار؛ الصفحة الحالية `.navlink.on` مبرزة بخلفية `var(--accent)` (بتتلوّن مع الصفحة). `updateNav()` تُستدعى من `go()` والبداية. الضغط = `go(i)`.
- **المحتوى موسّط:** `.page{justify-content:center}` + `.panel{text-align:center;flex column;align-items:center}` — كل شي فوق وتحت العنوان بالوسط.
- **اللوغو الثابت بالزاوية اتشال** (المستخدم طلب حذفه) — بقي فقط لوكاب `.page-brand` فوق عنوان كل صفحة.

### اللوغوهات + أيقونات اللغات (كلها تتلوّن مع الصفحة)
- **لوكاب فوق العنوان** (`.page-brand` = `.page-logo` كبير + `.page-brand-name` «Mohammed Al-Hussein»): يُحقن بـ `nav.js` (`LOGO_LOCKUP`) فوق `.panel` بكل الصفحات ما عدا الرئيسية؛ خطوط اللوغو `var(--accent)`.
- **كود يتكتب ذاتياً** (`.code-type`): يُحقن بـ `nav.js` من مصفوفة `SNIPPETS` (13، فاضي للرئيسية/Projects/Contact)؛ `typeCode(i)` بتكتبه حرف-حرف عند فتح الصفحة (تُستدعى من `go()`). لونه `var(--accent)` + مؤشّر وامض `::after`.
- **أيقونات اللغات** (`.lang-icon`): `-webkit-mask/mask` من Simple Icons + `background:var(--accent)`. اللينك بدون لون: `url(https://cdn.simpleicons.org/{slug})`.
- **مصفوفة ACCENT (13):** ألوان متباعدة عمداً — كل صفحتين متتاليتين مختلفتين بوضوح (تبديل دافئ/بارد). لا تحطّ لونين متشابهين بالتتالي.
- **الخلفية فضية ثابتة:** `background.js` لونه `SILVER=#c3ccd9` و`window.bgAccent` صار **no-op** — الخلفية مفصولة تماماً عن لون الصفحة (المستخدم حسّ بتأخير من تغيّر لون الخلفية، فثبّتناها). أي تغيّر لون هلق فوري (الروبوت + متغيّرات CSS بس).

### البهرجة (`fx.js`)
- **مقدّمة:** `.home-name` يتكتب حرف-حرف (setTimeout) + مؤشّر `.type-caret`. الروبوت دخول سينمائي عبر CSS (`#robot-container` opacity/scale → `body.robot-ready`).
- **عدّادات:** أي عنصر فيه `data-count` (و`data-prefix`) بيعدّ من ٠ للهدف بأرقام عربية (requestAnimationFrame). مثال: `+٦ سنين خبرة`.
- **إمالة 3D:** `.proj` (بطاقات المشاريع) تميل مع الماوس (mousemove → rotateX/Y). CSS: زجاجية `backdrop-filter` + حافّة متوهّجة دوّارة `::before` (conic-gradient + `@property --ang`).
- **تنبيه فحص:** `requestAnimationFrame` بيتوقّف لمّا تكون لوحة المعاينة مخفية، فالعدّاد بيبيّن ٠ بالفحص — بكروم الحقيقي بيشتغل.

### إضافة/حذف صفحة
لازم تزامن 3 أماكن **بنفس الترتيب**:
1. `index.html`: أضف `<section class="page">…</section>` بمكانها.
2. `js/nav.js`: أضف اسمها بـ `NAMES` + لون بـ `ACCENT`.
3. `js/robot.js`: أضف حركة بـ `PAGE_ANIM`.

### قوالب صفحة اللغة (نسخة/تعديل)
كل صفحة لغة: `lang-head` (لوغو + kicker + عنوان) + `arname` + `page-desc` + `level lvl-{1/2/3}` (مبتدئ/متوسط/متقدّم) + `tags`.

## تفضيلات المستخدم (مهمة)
- ينفّذ التعديلات **مباشرة بدون رسائل تأكيد**.
- **لا لقطات شاشة/تحقّق بصري إلا للضرورة** — المستخدم يراجع بنفسه.
- كل الموارد **مجانية** فقط.
- كيبورد المستخدم تركي (رموز `{ } [ ]` بدها AltGr) — هو مبتدئ نسبياً بالبرمجة، فاشرح بوضوح.

## مشاريع المستخدم الحقيقية (معروضة بصفحة Projects)
موقع Mozaik، عارضات علب 3D (Three.js)، نظام ERP «النسيج الذهبي»، CyberLearn (Flutter/Dart)، Network Load Tester (Flutter/Dart).

## تنبيه
مجلد `Desktop/my app` القديم كان يحتوي مشروع Flutter آخر اختلط معه هذا الموقع؛ لهيك انفصلنا لهذا المجلد النظيف. اشتغل هون فقط.
